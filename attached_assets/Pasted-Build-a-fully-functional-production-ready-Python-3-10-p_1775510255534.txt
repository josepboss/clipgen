Build a fully functional, production-ready Python 3.10 project that generates vertical short-form video clips from a YouTube video URL. The system must run end-to-end without placeholders or mocked logic.

GOAL:
Input a YouTube URL → automatically extract the best segments → generate vertical (9:16) short clips with face-centered cropping → save as MP4 files.

INPUT:
- Command line usage:
  python main.py "<youtube_url>"

DEPENDENCIES:
- pytube
- youtube-transcript-api
- opencv-python
- ffmpeg (must be invoked via subprocess, not wrappers)
- python-dotenv
- requests

TRANSCRIPT HANDLING:
1. Attempt to fetch transcript using youtube-transcript-api
2. If unavailable, fallback to Whisper API via OpenRouter
3. Output must be a list of timestamped text segments

SEGMENTATION:
- Combine transcript into continuous segments between 30 and 90 seconds
- Ensure segments are coherent (do not cut mid-sentence)

AI SCORING (MANDATORY):
- Use OpenRouter API (not OpenAI directly)
- Model priority:
  1. qwen/qwen3-32b (default)
  2. deepseek-chat (fallback)
- Send all segments in one request when possible

STRICT OUTPUT FORMAT:
The model MUST return valid JSON only:
[
  {
    "start": <seconds>,
    "end": <seconds>,
    "score": <0-10 float>,
    "reason": "<short explanation>"
  }
]

- Reject any non-JSON output
- Parse and select top 3–5 segments by score

VIDEO PROCESSING:
- Use FFmpeg via subprocess for all video operations
- Precisely cut clips using timestamps (no re-encoding drift)
- Convert each clip to vertical 1080x1920

FACE DETECTION:
- Use OpenCV Haar cascade for face detection
- Detect face per frame
- Apply smooth horizontal cropping to keep face centered
- If no face detected, fallback to center crop
- Ensure no black bars in final output

OUTPUT:
- Save all clips in /output directory
- Filenames:
  clip_01.mp4, clip_02.mp4, ...
- Encoding:
  - video: H.264
  - audio: AAC
- Ensure all files are playable and valid

PROJECT STRUCTURE:
- main.py (entry point, CLI handling)
- processor.py (pipeline orchestration)
- transcript.py (transcript extraction + fallback)
- ai_selector.py (OpenRouter API call + JSON parsing)
- video_editor.py (FFmpeg + OpenCV processing)
- utils.py (helpers)
- requirements.txt
- .env (API keys)

CONFIG:
- Load OPENROUTER_API_KEY from .env
- Do not hardcode secrets

ERROR HANDLING:
- Handle invalid YouTube URLs
- Handle missing transcripts
- Handle API failures (retry once, then fallback model)
- Handle FFmpeg errors
- Print clear logs for each step

CONSTRAINTS:
- No pseudo-code
- No placeholder implementations
- All modules must be fully implemented
- Must run on Linux VPS without modification
- Use subprocess for FFmpeg (no external wrappers)

FINAL REQUIREMENT:
After running:
  pip install -r requirements.txt
  python main.py "<youtube_url>"

The system must produce real, correctly formatted vertical video clips in the output folder without manual intervention.