{
  "name": "clipgen-publisher",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright-based TikTok publisher for ClipGen",
  "main": "scheduler.js",
  "scripts": {
    "install-browsers": "npx playwright install chromium --with-deps"
  },
  "dependencies": {
    "playwright": "^1.44.0"
  }
}
